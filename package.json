{
  "name": "sds-motion",
  "version": "1.0.0",
  "description": "Animation by class name. Production-ready UI animations.",
  "main": "dist/sds-motion.js",
  "style": "dist/sds-motion.css",
  "scripts": {
    "generate": "node scripts/generate-animations.js && node scripts/generate-docs.js",
    "build": "npm run generate"
  },
  "author": "Salkom Design Studio",
  "license": "MIT",
  "devDependencies": {
  }
}
